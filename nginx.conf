events {
    worker_connections 1024;
}

http {
    upstream api {
        least_conn;
        server api1:80;
        server api2:80;
    }
    
    server {
        listen 9999;

        location / {
            proxy_pass http://api;
        }
    }
}
